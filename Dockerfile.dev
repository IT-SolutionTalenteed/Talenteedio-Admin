# Dockerfile de développement avec hot reload
FROM node:20-alpine

WORKDIR /app

# Copie les fichiers de dépendances
COPY package*.json ./

# Installe toutes les dépendances (dev incluses)
RUN npm install

# Expose le port 5173 (Vite default)
EXPOSE 5173

# Installe les dépendances et démarre Vite
CMD ["sh", "-c", "npm install && npx vite --host 0.0.0.0"]
